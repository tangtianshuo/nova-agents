---
phase: 04-dialogs-quality-assurance
plan: 06
type: execute
wave: 4
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
  - 04-05
files_modified:
  - src/renderer/pages/Settings/sections/GeneralSection.tsx
  - src/renderer/pages/Settings/components/dialogs/CustomProviderDialog.tsx
  - src/renderer/pages/Settings/components/dialogs/CustomMcpDialog.tsx
  - src/renderer/pages/Settings/components/dialogs/PlaywrightConfigPanel.tsx
  - src/renderer/pages/Settings/components/dialogs/EdgeTtsConfigPanel.tsx
  - src/renderer/pages/Settings/components/dialogs/GeminiImageConfigPanel.tsx
  - src/renderer/pages/Settings/components/ProviderCard.tsx
  - src/renderer/pages/Settings/components/McpServerCard.tsx
  - src/renderer/pages/Settings/sections/ProviderSection.tsx
  - src/renderer/pages/Settings/sections/McpSection.tsx
  - src/renderer/pages/Settings/index.tsx
autonomous: true
requirements:
  - QA-01
  - QA-02
  - QA-03
  - QA-04
  - QA-05
  - QA-06
user_setup: []

must_haves:
  truths:
    - All dialog components pass TypeScript compilation (no any types)
    - All dialog components pass ESLint (no react-hooks/exhaustive-deps warnings)
    - All files <500 lines (wc -l check)
    - All Props interfaces documented with JSDoc comments
    - All functionality regression tested (no broken features)
    - All components follow React Stability Rules
    - Old Settings.tsx deleted (all features migrated)
  artifacts:
    - path: src/renderer/pages/Settings/ (entire directory structure)
      provides: Complete Settings page with all sections and dialogs
      contains: sections/, components/, components/dialogs/
  key_links:
    - from: All Settings components
      to: src/renderer/pages/Settings/index.tsx
      via: section routing and dialog overlays
      pattern: "Settings.*index.tsx.*imports.*all.*components"
---

<objective>
Execute comprehensive quality verification and cleanup for Phase 04.

Purpose: Ensure all dialog components and GeneralSection meet quality standards (TypeScript strict mode, ESLint rules, file size limits, regression testing). Delete old Settings.tsx and complete Settings componentization.

Output: Verified, production-ready Settings page with all components meeting quality gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-dialogs-quality-assurance/04-RESEARCH.md
@.planning/phases/04-dialogs-quality-assurance/04-UI-SPEC.md

@specs/tech_docs/react_stability_rules.md
</execution_context>

<context>
<interfaces>
<!-- Quality requirements from REQUIREMENTS.md -->
QA-01: All functionality regression tested (no broken features)
QA-02: TypeScript strict mode, no any types
QA-03: Single file <500 lines
QA-04: ESLint react-hooks/exhaustive-deps passes
QA-05: All components have explicit Props interfaces
QA-06: Follow React Stability Rules

<!-- React Stability Rules reference -->
From specs/tech_docs/react_stability_rules.md:
1. Context Separation: Each dialog manages its own form state
2. useEffect Dependencies: Validate dependencies include all reactive values
3. Event Handler Stability: Wrap onSave, onCancel callbacks in useCallback
4. No Inline Functions: Define handlers outside JSX
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript strict mode verification</name>
  <files>
    - src/renderer/pages/Settings/sections/GeneralSection.tsx
    - src/renderer/pages/Settings/components/dialogs/CustomProviderDialog.tsx
    - src/renderer/pages/Settings/components/dialogs/CustomMcpDialog.tsx
    - src/renderer/pages/Settings/components/dialogs/PlaywrightConfigPanel.tsx
    - src/renderer/pages/Settings/components/dialogs/EdgeTtsConfigPanel.tsx
    - src/renderer/pages/Settings/components/dialogs/GeminiImageConfigPanel.tsx
    - src/renderer/pages/Settings/components/ProviderCard.tsx
    - src/renderer/pages/Settings/components/McpServerCard.tsx
    - src/renderer/pages/Settings/sections/ProviderSection.tsx
    - src/renderer/pages/Settings/sections/McpSection.tsx
    - src/renderer/pages/Settings/index.tsx
  </files>
  <read_first>
    - All Settings component files (verify no any types)
  </read_first>
  <action>
1. Run TypeScript compiler:
```bash
npm run typecheck
```

2. If errors exist, fix them:
- Add missing type annotations
- Remove `any` types, use proper types instead
- Fix implicit any errors in event handlers
- Ensure all Props interfaces are complete

3. Search for any remaining `any` usage:
```bash
grep -rn ": any" src/renderer/pages/Settings/
grep -rn "any\[\]" src/renderer/pages/Settings/
```

4. Fix any `any` types found:
- Replace `any` with specific type
- Use proper type definitions from @/config/types
- Create new type interfaces if needed

5. Verify all Props interfaces have JSDoc comments:
```bash
grep -rn "export interface.*Props" src/renderer/pages/Settings/ | while read line; do
  file=$(echo $line | cut -d: -f1);
  # Check if interface has JSDoc comment above it
done
```

6. Add JSDoc comments to Props interfaces:
```typescript
/**
 * ComponentName - Brief description
 *
 * Detailed description of what the component does.
 * Include usage examples if applicable.
 */
export interface ComponentNameProps {
  /** prop description */
  propName: string;
}
```
  </action>
  <verify>
    <automated>
npm run typecheck 2>&1 | tee /tmp/typecheck.log
grep -i "error TS" /tmp/typecheck.log && exit 1 || echo "TypeScript compilation passed"
grep -rn ": any" src/renderer/pages/Settings/ | grep -v "node_modules" && exit 1 || echo "No any types found"
grep -rn "export interface.*Props" src/renderer/pages/Settings/ | wc -l
    </automated>
  </verify>
  <done>
TypeScript compilation passes with zero errors
No `any` types found in Settings components
All Props interfaces have JSDoc comments
All type definitions are explicit and correct
  </done>
</task>

<task type="auto">
  <name>Task 2: ESLint rules verification</name>
  <files>
    - src/renderer/pages/Settings/sections/GeneralSection.tsx
    - src/renderer/pages/Settings/components/dialogs/CustomProviderDialog.tsx
    - src/renderer/pages/Settings/components/dialogs/CustomMcpDialog.tsx
    - src/renderer/pages/Settings/components/dialogs/PlaywrightConfigPanel.tsx
    - src/renderer/pages/Settings/components/dialogs/EdgeTtsConfigPanel.tsx
    - src/renderer/pages/Settings/components/dialogs/GeminiImageConfigPanel.tsx
    - src/renderer/pages/Settings/components/ProviderCard.tsx
    - src/renderer/pages/Settings/components/McpServerCard.tsx
    - src/renderer/pages/Settings/sections/ProviderSection.tsx
    - src/renderer/pages/Settings/sections/McpSection.tsx
    - src/renderer/pages/Settings/index.tsx
  </files>
  <read_first>
    - All Settings component files (verify ESLint compliance)
  </read_first>
  <action>
1. Run ESLint:
```bash
npm run lint 2>&1 | tee /tmp/eslint.log
```

2. If errors exist, fix them:
- Fix react-hooks/exhaustive-deps warnings
- Add missing dependencies to useEffect/useCallback/useMemo
- Remove unused variables and imports
- Fix no-console warnings (use logger instead)

3. Common fixes for react-hooks/exhaustive-deps:
```typescript
// ❌ Wrong - missing dependency
useEffect(() => {
  const handler = () => console.log('click');
  window.addEventListener('click', handler);
  return () => window.removeEventListener('click', handler);
}, []); // Missing dependency

// ✅ Correct - useCallback
const handler = useCallback(() => {
  console.log('click');
}, []);

useEffect(() => {
  window.addEventListener('click', handler);
  return () => window.removeEventListener('click', handler);
}, [handler]);
```

4. Verify no console.log statements in production code:
```bash
grep -rn "console.log" src/renderer/pages/Settings/ | grep -v "node_modules"
```

5. Replace console.log with proper logging:
```typescript
// Use logger or remove debug logs
import { logger } from '@/utils/logger';
logger.info('[ComponentName] Message');
```

6. Re-run ESLint until all warnings resolved
  </action>
  <verify>
    <automated>
npm run lint 2>&1 | tee /tmp/eslint-final.log
grep -i "warning\|error" /tmp/eslint-final.log && exit 1 || echo "ESLint passed with no warnings"
grep -rn "console.log" src/renderer/pages/Settings/ | grep -v "node_modules" | grep -v "// console.log" && exit 1 || echo "No console.log found"
    </automated>
  </verify>
  <done>
ESLint passes with zero warnings
No react-hooks/exhaustive-deps warnings
All useEffect/useCallback/useMemo have correct dependencies
No unused variables or imports
No console.log statements (or only in comments)
  </done>
</task>

<task type="auto">
  <name>Task 3: File size verification</name>
  <files>
    - src/renderer/pages/Settings/sections/GeneralSection.tsx
    - src/renderer/pages/Settings/components/dialogs/CustomProviderDialog.tsx
    - src/renderer/pages/Settings/components/dialogs/CustomMcpDialog.tsx
    - src/renderer/pages/Settings/components/dialogs/PlaywrightConfigPanel.tsx
    - src/renderer/pages/Settings/components/dialogs/EdgeTtsConfigPanel.tsx
    - src/renderer/pages/Settings/components/dialogs/GeminiImageConfigPanel.tsx
    - src/renderer/pages/Settings/components/ProviderCard.tsx
    - src/renderer/pages/Settings/components/McpServerCard.tsx
    - src/renderer/pages/Settings/sections/ProviderSection.tsx
    - src/renderer/pages/Settings/sections/McpSection.tsx
    - src/renderer/pages/Settings/index.tsx
  </files>
  <read_first>
    - All Settings component files (verify file sizes)
  </read_first>
  <action>
1. Check file sizes:
```bash
wc -l src/renderer/pages/Settings/sections/GeneralSection.tsx
wc -l src/renderer/pages/Settings/components/dialogs/*.tsx
wc -l src/renderer/pages/Settings/components/*.tsx
wc -l src/renderer/pages/Settings/sections/*.tsx
wc -l src/renderer/pages/Settings/index.tsx
```

2. Create file size report:
```bash
echo "Settings File Size Report"
echo "=========================="
find src/renderer/pages/Settings -name "*.tsx" -exec wc -l {} \; | sort -rn
```

3. If any file >500 lines, split it:
- Extract sub-components to separate files
- Split large sections into multiple components
- Extract constants/config to separate files
- Extract utility functions to separate files

4. Example split for >500 line file:
```typescript
// Before: LargeComponent.tsx (600 lines)
// After: LargeComponent.tsx (350 lines) + LargeComponentSubComponent.tsx (200 lines) + LargeComponentConstants.ts (50 lines)
```

5. Re-check file sizes after splits
  </action>
  <verify>
    <automated>
wc -l src/renderer/pages/Settings/sections/GeneralSection.tsx | awk '{if ($1 > 500) exit 1}'
wc -l src/renderer/pages/Settings/components/dialogs/*.tsx | awk '{if ($1 > 500) exit 1}'
wc -l src/renderer/pages/Settings/components/*.tsx | awk '{if ($1 > 500) exit 1}'
wc -l src/renderer/pages/Settings/sections/*.tsx | awk '{if ($1 > 500) exit 1}'
wc -l src/renderer/pages/Settings/index.tsx | awk '{if ($1 > 500) exit 1}'
find src/renderer/pages/Settings -name "*.tsx" -exec wc -l {} \; | awk '{if ($1 > 500) print $0}'
    </automated>
  </verify>
  <done>
All Settings component files <500 lines
File size report created
No files exceed size limit
All large files have been split if needed
  </done>
</task>

<task type="auto">
  <name>Task 4: React Stability Rules compliance check</name>
  <files>
    - src/renderer/pages/Settings/sections/GeneralSection.tsx
    - src/renderer/pages/Settings/components/dialogs/CustomProviderDialog.tsx
    - src/renderer/pages/Settings/components/dialogs/CustomMcpDialog.tsx
    - src/renderer/pages/Settings/components/dialogs/PlaywrightConfigPanel.tsx
    - src/renderer/pages/Settings/components/dialogs/EdgeTtsConfigPanel.tsx
    - src/renderer/pages/Settings/components/dialogs/GeminiImageConfigPanel.tsx
    - src/renderer/pages/Settings/components/ProviderCard.tsx
    - src/renderer/pages/Settings/components/McpServerCard.tsx
    - src/renderer/pages/Settings/sections/ProviderSection.tsx
    - src/renderer/pages/Settings/sections/McpSection.tsx
    - src/renderer/pages/Settings/index.tsx
  </files>
  <read_first>
    - All Settings component files (verify React Stability Rules)
    - specs/tech_docs/react_stability_rules.md (reference)
  </read_first>
  <action>
1. Verify Context Separation:
```bash
# Each dialog should manage its own form state
grep -rn "useState.*formData\|form\|dialog" src/renderer/pages/Settings/components/dialogs/*.tsx
# Verify no global dialog state pollution
```

2. Verify useEffect Dependencies:
```bash
# Check all useEffect have complete dependency arrays
grep -A 5 "useEffect" src/renderer/pages/Settings/**/*.tsx | grep -E "^\s*\],?\s*\["
```

3. Verify Event Handler Stability:
```bash
# Check all handlers use useCallback
grep -rn "const handle.*=.*useCallback" src/renderer/pages/Settings/**/*.tsx
```

4. Verify No Inline Functions:
```bash
# Check for inline function definitions in JSX
grep -rn "onClick={.*=>" src/renderer/pages/Settings/**/*.tsx | grep -v "useCallback"
```

5. Create compliance report:
```typescript
// React Stability Rules Compliance Checklist
// Context Separation: ✅ Each dialog manages its own form state
// useEffect Dependencies: ✅ All useEffect have complete deps
// Event Handler Stability: ✅ All handlers use useCallback
// No Inline Functions: ✅ No inline functions in JSX
```

6. Fix any violations:
- Add missing dependencies to useEffect
- Wrap inline functions in useCallback
- Lift state to appropriate component level
  </action>
  <verify>
    <automated>
grep -rn "useCallback" src/renderer/pages/Settings/**/*.tsx | wc -l
grep -rn "onClick={.*=>" src/renderer/pages/Settings/**/*.tsx | grep -v "useCallback" | grep -v "//" | wc -l
grep -A 3 "useEffect" src/renderer/pages/Settings/**/*.tsx | grep -c "^\s*\],?\s*\["
    </automated>
  </verify>
  <done>
Context Separation verified (all dialogs manage own state)
useEffect dependencies verified (all complete)
Event handlers verified (all use useCallback)
No inline functions in JSX
React Stability Rules compliance report created
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 5: Functional regression testing</name>
  <files>
    - src/renderer/pages/Settings/ (entire Settings page)
  </files>
  <read_first>
    - All Settings component files (verify functionality)
    - .planning/phases/04-dialogs-quality-assurance/04-UI-SPEC.md (QA checklist)
  </read_first>
  <what-built>
Complete Settings page with all sections and dialogs:
- GeneralSection (startup, theme, workspace settings)
- ProviderSection (provider management with CustomProviderDialog)
- McpSection (MCP management with CustomMcpDialog)
- AccountSection (user account settings)
- AboutSection (app info and QR code)
- DeleteConfirmDialog (reusable delete confirmation)
- PlaywrightConfigPanel (Playwright MCP settings)
- EdgeTtsConfigPanel (Edge TTS settings)
- GeminiImageConfigPanel (Gemini Image settings)
  </what-built>
  <how-to-verify>
1. Start Tauri dev: npm run tauri:dev
2. Navigate to Settings page (click gear icon or Settings menu item)
3. Test GeneralSection:
   - Toggle auto-start (should show loading, then toast)
   - Toggle minimize to tray (immediate, toast)
   - Switch theme (system/light/dark, should apply immediately)
   - Change default workspace (should save selection)
4. Test ProviderSection:
   - Click "添加" button (should open CustomProviderDialog in add mode)
   - Fill form (name, cloud provider, protocol, URL, model)
   - Try saving with empty required fields (should show validation errors)
   - Switch protocol to OpenAI (should show warning)
   - Click cancel (dialog closes, no changes)
   - Open dialog again, fill valid data, save (dialog closes, toast shows)
5. Test McpSection:
   - Click "添加" button (should open CustomMcpDialog in add mode)
   - Select transport type (STDIO/HTTP/SSE)
   - Fill form (ID, name, command)
   - Toggle to JSON mode (should show JSON textarea)
   - Paste valid JSON (should parse successfully)
   - Switch back to form mode (should preserve data)
   - Save (dialog closes, toast shows)
   - Click builtin MCP "设置" button (should open config panel)
6. Test DeleteConfirmDialog:
   - Click delete icon on provider card (should open dialog)
   - Click cancel (dialog closes, no action)
   - Click delete icon again, click delete (should show loading, then close)
7. Test all sections navigation (sidebar should switch sections correctly)
8. Verify all features work as before (no regressions)
  </how-to-verify>
  <resume-signal>
Type "approved" if all features work correctly, or describe issues found.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 6: Delete old Settings.tsx and finalize</name>
  <files>
    - src/renderer/pages/Settings/Settings.tsx.bak (backup, if exists)
    - .planning/phases/04-dialogs-quality-assurance/04-06-SUMMARY.md (summary)
  </files>
  <read_first>
    - src/renderer/pages/Settings/index.tsx (verify all imports correct)
    - All component files (verify all features migrated)
  </read_first>
  <action>
1. Verify all features have been migrated:
- Check old Settings.tsx (if exists) for any remaining functionality
- Ensure all sections extracted (Account, About, Provider, MCP, General)
- Ensure all dialogs extracted (DeleteConfirm, CustomProvider, CustomMcp, builtin panels)
- Ensure all cards extracted (ProviderCard, McpServerCard)

2. If old Settings.tsx exists, backup it:
```bash
mv src/renderer/pages/Settings/Settings.tsx src/renderer/pages/Settings/Settings.tsx.bak
```

3. Verify no remaining imports reference old Settings.tsx:
```bash
grep -rn "from.*Settings\.tsx" src/renderer/ --exclude-dir=node_modules
```

4. Update any remaining imports to use new structure:
```typescript
// Old import
import { Settings } from '@/pages/Settings/Settings';

// New import
import Settings from '@/pages/Settings';
```

5. Create final directory structure summary:
```bash
tree -L 3 src/renderer/pages/Settings/
```

6. Create component inventory:
```bash
echo "Settings Component Inventory"
echo "============================"
echo "Sections:"
ls -1 src/renderer/pages/Settings/sections/
echo "Dialogs:"
ls -1 src/renderer/pages/Settings/components/dialogs/
echo "Components:"
ls -1 src/renderer/pages/Settings/components/
echo "Total files:"
find src/renderer/pages/Settings -name "*.tsx" | wc -l
```

7. Generate final statistics:
```bash
echo "Settings Refactoring Statistics"
echo "================================"
echo "Total lines of code:"
find src/renderer/pages/Settings -name "*.tsx" -exec wc -l {} + | tail -1
echo "Number of components:"
find src/renderer/pages/Settings -name "*.tsx" | wc -l
echo "Average lines per file:"
find src/renderer/pages/Settings -name "*.tsx" -exec wc -l {} + | tail -1 | awk '{print $1}' | xargs -I {} echo {} / $(find src/renderer/pages/Settings -name "*.tsx" | wc -l) | bc
```
  </action>
  <verify>
    <automated>
ls src/renderer/pages/Settings/Settings.tsx 2>/dev/null && exit 1 || echo "Old Settings.tsx deleted or never existed"
grep -rn "from.*Settings\.tsx" src/renderer/ --exclude-dir=node_modules && exit 1 || echo "No old imports found"
find src/renderer/pages/Settings -name "*.tsx" | wc -l
find src/renderer/pages/Settings -name "*.tsx" -exec wc -l {} + | tail -1
    </automated>
  </verify>
  <done>
Old Settings.tsx backed up (if existed)
No remaining imports reference old Settings.tsx
All features verified migrated
Component inventory created
Final statistics generated
Settings refactoring complete
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation passes (npm run typecheck)
2. ESLint passes with no warnings (npm run lint)
3. All files <500 lines (wc -l check)
4. All Props interfaces documented with JSDoc
5. All components follow React Stability Rules
6. All functionality regression tested (human verification)
7. Old Settings.tsx deleted or backed up
8. Component inventory created
9. Final statistics generated
</verification>

<success_criteria>
1. All QA requirements met (QA-01 through QA-06)
2. Zero TypeScript compilation errors
3. Zero ESLint warnings
4. All files <500 lines
5. All components properly documented
6. All features work correctly (no regressions)
7. Settings refactoring complete
8. Phase 04 successfully completed
</success_criteria>

<output>
After completion, create `.planning/phases/04-dialogs-quality-assurance/04-06-SUMMARY.md` with:
- Quality verification results
- File size report
- Component inventory
- Final statistics
- Any issues found and resolved
- Phase completion status
</output>
