---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/pages/Settings/SettingsLayout.tsx
  - src/renderer/pages/Settings/index.ts
autonomous: true
requirements:
  - ARCH-02
must_haves:
  truths:
    - "SettingsLayout component renders two-column layout (sidebar + content area)"
    - "Layout is responsive with sidebar on mobile configurable"
    - "Component receives activeSection and onSectionChange as props"
    - "No business logic in layout component (pure container)"
  artifacts:
    - path: "src/renderer/pages/Settings/SettingsLayout.tsx"
      provides: "Layout container component"
      min_lines: 100
      exports: ["default function SettingsLayout"]
      contains: "interface SettingsLayoutProps"
    - path: "src/renderer/pages/Settings/index.ts"
      provides: "Entry point that imports SettingsLayout"
      min_lines: 20
      exports: ["default function Settings"]
  key_links:
    - from: "SettingsLayout.tsx"
      to: "SettingsSidebar.tsx"
      via: "Import and render SettingsSidebar component"
      pattern: "import.*SettingsSidebar.*from.*SettingsSidebar"
    - from: "SettingsLayout.tsx"
      to: "index.tsx"
      via: "Composition root uses SettingsLayout"
      pattern: "SettingsLayout.*activeSection.*onSectionChange"
---

<objective>
Create SettingsLayout component as the layout container for the Settings page.

Purpose: Provide a clean two-column layout (sidebar + content area) that manages the structural layout without business logic. This component will be used by the composition root (Settings/index.tsx) to render all Settings sections.

Output: Working SettingsLayout component (~150 lines) with TypeScript Props interface, responsive layout, and sidebar/content area rendering.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/settings-componentization.md
@specs/guides/design_guide.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SettingsLayout component with TypeScript interface</name>
  <files>src/renderer/pages/Settings/SettingsLayout.tsx, src/renderer/pages/Settings/index.ts</files>
  <read_first>
    src/renderer/pages/Settings.tsx (lines 1-100 for import patterns and layout structure)
    docs/settings-componentization.md (§4.1 for SettingsLayout Props interface)
  </read_first>
  <action>
    Create SettingsLayout component as a pure layout container:

    **File**: src/renderer/pages/Settings/SettingsLayout.tsx

    1. Import React and necessary dependencies:
       ```typescript
       import React from 'react';
       import type { SettingsSection } from '@/config/types'; // Import SettingsSection type from existing types
       import type { AppConfig } from '@/config/types'; // Import AppConfig from existing types
       ```

    2. Define SettingsLayoutProps interface (from design doc §4.1):
       ```typescript
       export interface SettingsLayoutProps {
         /** Currently active section */
         activeSection: SettingsSection;
         /** Callback when section changes */
         onSectionChange: (section: SettingsSection) => void;
         /** Content to render in main area */
         children: React.ReactNode;
         /** App configuration */
         config: AppConfig;
       }
       ```

    3. Create SettingsLayout component:
       ```typescript
       export default function SettingsLayout({
         activeSection,
         onSectionChange,
         children,
         config,
       }: SettingsLayoutProps) {
         // Render layout container
         return (
           <div className="flex h-full bg-[var(--paper)]">
             {/* Sidebar will be rendered here in next plan */}
             <div className="w-52 flex-shrink-0 border-r border-[var(--line)]">
               {/* SettingsSidebar placeholder */}
               <div className="p-4 text-[var(--ink-muted)]">
                 Sidebar (will be SettingsSidebar)
               </div>
             </div>

             {/* Main content area */}
             <main className="flex-1 overflow-y-auto">
               <div className="max-w-5xl mx-auto p-6">
                 {children}
               </div>
             </main>
           </div>
         );
       }
       ```

    4. Create entry point file src/renderer/pages/Settings/index.ts:
       ```typescript
       import SettingsLayout from './SettingsLayout';

       export default function Settings() {
         return (
           <div className="h-full">
             <SettingsLayout
               activeSection="general"
               onSectionChange={() => {}}
               config={{} as any}
             >
               <div>Settings content placeholder</div>
             </SettingsLayout>
           </div>
         );
       }
       ```

    **Key design decisions**:
    - Pure layout container (no business logic)
    - Two-column layout: sidebar (fixed w-52) + content area (flex-1)
    - Content area uses max-w-5xl and mx-auto for centered reading width
    - Children prop allows flexible content rendering
    - Uses CSS variables from design system (--paper, --line)
    - Per D-02: SettingsLayout is pure container, no state management
  </action>
  <verify>
    <automated>grep -q "export interface SettingsLayoutProps" src/renderer/pages/Settings/SettingsLayout.tsx && grep -q "activeSection: SettingsSection" src/renderer/pages/Settings/SettingsLayout.tsx && grep -q "export default function SettingsLayout" src/renderer/pages/Settings/SettingsLayout.tsx && echo "SettingsLayout component created with correct interface"</automated>
  </verify>
  <done>
    SettingsLayout.tsx exists with:
    - SettingsLayoutProps interface with activeSection, onSectionChange, children, config
    - Two-column layout (sidebar + content area)
    - Pure container component (no business logic)
    - index.ts imports and renders SettingsLayout with placeholder content
  </done>
</task>

</tasks>

<verification>
After completion, verify:
1. SettingsLayoutProps interface exists with all required fields
2. Component renders two-column layout structure
3. No business logic or state management in layout component
4. index.ts imports and uses SettingsLayout correctly
</verification>

<success_criteria>
- [ ] SettingsLayout.tsx created with ~150 lines
- [ ] TypeScript interface defines activeSection, onSectionChange, children, config props
- [ ] Layout renders two-column structure (sidebar + content area)
- [ ] Component is pure container with no business logic
- [ ] index.ts entry point created and imports SettingsLayout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
